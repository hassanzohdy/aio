Config.extend({
    http: {
        endpoint: {
            baseUrl: 'http://aio.mentoor.io/api',
            apiKey: 'ASDP11ZP23RDCSDZXOPP23', // api key if required i.e Authorization: key {some-key}, add only the key without the `key` word
        }
    },
});